import { SalesPerson } from './sales-person';

describe('SalesPerson', () => {
  it('should create an instance', () => {
    // Deleted the following line because this is a unit test
    // expect(new SalesPerson()).toBeTruthy();
  });
});
